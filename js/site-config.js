/**
 * Site toggles — edit this file to change behaviour without touching page logic.
 */
window.SITE_CONFIG = {
  /** Set to false to disable the home-page helper entirely. */
  homeGuideEnabled: false,

  /** Seconds without mouse, keyboard, scroll, or touch before the helper appears. */
  homeGuideIdleSeconds: 30,

  /** Set to true to show the testimonials block on .creative (hidden while false). */
  testimonialsEnabled: false,
};
